# Cloud-only features (billing, credits, user management).
# This stub package is used when BILLING_ENABLED=false (default for self-hosted).
# In the hosted version, this package is replaced by the private cloud implementation.
